"""Garak � LLM Vulnerability Scanner (NVIDIA).

"nmap for LLMs". Install, configure, run scans, interpret results.
Runs as CLI: `garak --model_type openai --model_name gpt-4 --probes all`

Default target: Level 2's vulnerable app (level-2-prompt-injection/vulnerable_app/).
Start the app first: `cd ../level-2-prompt-injection && uvicorn vulnerable_app.main:app`
"""
# TODO: implement
