# Notes — 02

## Guard list (from `__transformed_code_for_*.py`, un-broken version)
[paste the guards. Then annotate: which are type checks, which are shape checks, which are other attribute checks]

## FX graph nodes (from `__compiled_fn_*.py`)
[list each node, map to a line in tiny_model_dump.py's forward()]

## Diff between un-broken and broken
[how many compiled fns? how many transformed_code files? what changed in the dispatcher?]

## What surprised me
[anything]
