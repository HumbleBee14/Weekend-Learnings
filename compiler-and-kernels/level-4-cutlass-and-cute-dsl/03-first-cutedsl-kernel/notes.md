# Notes — 03

Capture as you go:

- The exact `pip install nvidia-cutlass-dsl` version you used; toolchain compile time on the first launch.
- The error message you got when you tried to call a `@cute.kernel` directly from Python.
- Your unswizzled vs swizzled transpose numbers on your hardware. Note the GPU model.
- One thing about the API that surprised you (good or bad).
